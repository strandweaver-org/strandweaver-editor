h1 Listing scripts

table
  thead
    tr
      th Title
      th Data
      th
      th
      th

  tbody
    - @scripts.each do |script|
      tr
        td = script.title
        td = script.data
        td = link_to 'Show', script
        td = link_to 'Edit', edit_script_path(script)
        td = link_to 'Destroy', script, data: { confirm: 'Are you sure?' }, method: :delete

br

= link_to 'New Script', new_script_path
